<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>

  <body class="body <%= "home-background-image" if current_page?(controller: "pages", action: "home") %>

    <div class="container container-fluid">
      <% flash.each do |name, msg| %>
        <br>
        <%= content_tag(:div, msg, class: "alert alert-info") %>
      <% end %>

      <%= render 'layouts/navbar' %>

      <%= yield %>

    </div> <!-- END container -->

  </body>
</html>


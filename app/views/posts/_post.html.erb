<li><%= post.created_at.to_time.strftime('%B %d %Y') %> :: 
	<%= link_to post.title, post %>
    <% if current_user %>::
      <%= link_to 'Edit', edit_post_path(post) %>
      <%= link_to 'Destroy', post, method: :delete, 
      						data: { confirm: 'Are you sure?' } %>
  <% end %>
</li>
